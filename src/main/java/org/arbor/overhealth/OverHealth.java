package org.arbor.overhealth;

import net.fabricmc.api.ModInitializer;

public class OverHealth implements ModInitializer {
    /**
     * Runs the mod initializer.
     */
    public static final String modId = "overhealth";

    @Override
    public void onInitialize() {
        //ModLoadingContext.registerConfig(modId, ModConfig.Type.CLIENT, Config.CLIENT_SPEC);
    }
}
